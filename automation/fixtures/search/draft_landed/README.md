# Draft-to-landed miner fixtures

These fixtures are synthetic and carry no claim about an original historical
permuter lineage.  Every source endpoint has a content hash, and every landing
endpoint is stored in an immutable commit snapshot by the focused tests.
